Higher cognition in monkeys:
working memory: 
- strategic organization of information (prior knowledge as a scaffold)
	- build the scaffold on the fly
		- sequencing, chunking, etc. 
- "rearrangements of information": is not learned with memorization. this is "manipulation" 

DLPFC: 
- WM, strategies, etc. 
- task: game = to visit targets without repeat
	- remember and update (integrate on the abstract goal space). 
- thing: arbitrary order and stick with it. (tendency to sequence the target: find probability of targets per sacade --> reorder the matrix to find a metric of the behavior's sequentiality)


variability across blocks: 
- how is "sequencing" buried in the neural activity?
- DLPFC neurons: "spatial tuning" (is this stable across blocks for example?)
- tuning lost when the behavior is less sequenced

not unique spatial information so much as general property of the task (a strategy)

simultaneous recordings: organization of a task map

in blocks where single neurons are less tuned, loads of things are in the population. 

with sequencing: population code shifts from single-neuron codes to distributed reps. 

ANNs: distributed codes --> robustness & generalizability

bio systems to implement different coding schemes. 

also, chunking + graph theory to identify chunked groups
